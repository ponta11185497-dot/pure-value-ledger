# プレ値台帳プロジェクト

このフォルダで作業を始めるときは、**必ず最初に**以下を確認してから着手すること。

1. `触る必要なし\STATUS.md` を読み、これまでの経緯・完了済みの機能・過去に解決した問題を把握する
2. `git log --oneline -20` で直近の変更履歴を確認する

これにより、ユーザーから「前回の続きから」と言われた場合でも、過去のやり取りの詳細を聞き直さずに状況を踏まえて作業できるようにする。

## 運用ルール

- ユーザーに関わる重要な決定・仕様変更・ハマった問題の解決策があった場合は、都度 `触る必要なし\STATUS.md` を更新し、次回のセッションに引き継ぐこと
- `起動.bat` はこのフォルダ専用のClaude Code起動アイコン。中身をむやみに変更・削除しない(過去に一度誤って削除し、ユーザーから復元を依頼された経緯がある)
- ユーザーが普段触る必要のないファイルは、以下のいずれかの方法で目に入らないようにすること
  - フォルダを移動しても動作に支障がないもの → `触る必要なし` フォルダへ移動
  - `.github` や `data.json` / `sources.json` のようにパス依存で移動できないもの → Windowsの隠しファイル属性(`attrib +h`)を付けてエクスプローラー上から見えなくする
  - `CLAUDE.md` もこのルールに従い隠しファイル化している(フォルダ直下にないと自動読み込みされないため移動不可)
- ユーザーが通常エクスプローラーで目にしてよいのは次のものだけにする: `プレ値台帳.html`(アプリ本体)、`起動.bat`(Claude Code起動)、`情報源を取り込む.bat`(情報源反映)、`新しい情報源.txt`(URL記入用)、`触る必要なし`フォルダ
